@model IEnumerable<ProjetGestionHotel1.Models.images>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.image_file)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.chambre.disponibilite)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.image_file)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.chambre.disponibilite)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_img }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_img }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_img })
        </td>
    </tr>
}

</table>
